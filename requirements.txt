# CropBasis — Streamlit Cloud requirements
# ALL packages below have prebuilt binary wheels for Python 3.14 (cp314).
# Nothing here requires a source build, GDAL, PROJ, or system libraries.
#
# Key changes vs previous attempts:
#   - fiona REMOVED: no cp314 wheel, always source-builds against GDAL.
#     geopandas 1.x uses pyogrio as its default I/O backend — fiona not needed.
#   - pyogrio added: binary wheel, replaces fiona for all vector I/O.
#   - All versions pinned to confirmed cp314 manylinux wheels.

streamlit>=1.35.0

# Geospatial — all have cp314 binary wheels
geopandas==1.1.3
shapely==2.1.2
pyproj==3.7.2
pyogrio==0.12.1

# Satellite / Planetary Computer
pystac-client>=0.8.0
planetary-computer==1.0.0
rasterio==1.5.0

# CDL pixel alignment (nearest-neighbour resize 30m CDL → 10m S2 grid)
scipy==1.17.1

# Futures data
yfinance>=0.2.40

# Data / numerics
pandas==3.0.3
numpy==2.4.4

# Forecasting
statsmodels==0.14.6

# Visualisation
plotly>=5.22.0

# HTTP (CropScape WCS CDL fallback)
requests>=2.31.0
