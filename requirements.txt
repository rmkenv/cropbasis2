# CropBasis — Streamlit Cloud requirements
# Python 3.11 required (pinned in runtime.txt)
# Every package here has a prebuilt cp311-manylinux wheel — nothing compiles from source

streamlit>=1.35.0

# Geospatial
geopandas==1.0.1
shapely==2.0.6
pyproj==3.7.2
fiona==1.9.6

# Satellite / Planetary Computer
pystac-client==0.8.3
planetary-computer==1.0.0
rasterio==1.3.11

# CDL pixel alignment (nearest-neighbour resize 30m CDL → 10m S2 grid)
scipy==1.13.1

# Futures data
yfinance==0.2.40

# Data / numerics
pandas==2.2.2
numpy==1.26.4

# Forecasting
statsmodels==0.14.2

# Visualisation
plotly==5.22.0

# HTTP (CropScape WCS CDL fallback)
requests==2.32.3
